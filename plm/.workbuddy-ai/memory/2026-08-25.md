# 2026-08-25 Work Log

## pipe_lane_mapper_top hierarchy split (completed)

Split the monolithic `pipe_lane_mapper_top` (was 383 lines flat) into 4 modules:

1. **pipe_lane_clk_top** (gen_clk_top): decoder + ctrl_pclk + group BBM + feedback clock mux
   - Added `output lane_sel_t sel_tgt` port so top can use it for sel_tgt passthrough signals
   - Moved `lane_sel_t sel_tgt` from internal signal to output port

2. **pipe_lane_data_m2p** (gen_data_m2p): mac2phy data mux, per-lane onehot_mux

3. **pipe_lane_data_p2m** (gen_data_p2m): phy2mac data mux, by controller port

4. **pipe_lane_mapper_top** (gen_top): pure instantiation layer (103 lines)
   - Only port declarations + lane_sel_t sel/sel_tgt wires + 3 sub-module instances
   - sel_tgt passthrough signal logic stays at top (if any sel_tgt signals exist)

Also updated:
- gen_filelist: gen list now has 6 files in dependency order
- main(): files dict now generates 8 outputs (6 SV + 2 filelists)
- gen_clk_top: added sel_tgt output port

Verification: `python tools/plm_gen.py --quiet` exits 0, all 8 files generated correctly.

## Documentation (completed)

Wrote `docs/changelog_2026-08-25.md` covering all changes in this session:
- Config changes (controllers.csv, lane_mapping.csv, pipe_signals.csv)
- Python generator changes (CSV strip, feedback clk dedup, gen_decoder cleanup, phy_rst_n evolution)
- Hierarchy split (383-line flat -> 4 modules: clk_top / data_m2p / data_p2m / mapper_top)
- Lane grouping results, module interfaces, key design decisions
