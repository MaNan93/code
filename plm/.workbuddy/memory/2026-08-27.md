# 2026-08-27

## Verilator + GTKWave 仿真验证（--sel-mode comb）

- 用 `tools/plm_gen.py --config config --sel-mode comb` 重新生成 RTL，Verilator 5.050（conda env: `C:/Users/man/miniforge3/envs/verilator/Library`）仿真通过：TB_RESULT PASS，4 mode 路由 + SAFE 态 + one-hot + 复位脉冲全 0 错误，4us 仿真 0.18s。
- **本机 Verilator 工具链三大坑**（已固化到 `run_sim.sh`）：
  1. 包内 `verilated.mk` 硬编码 conda-forge CI 机器的 cl.exe 路径（本机无 VS2022），`verilator --binary` 自动构建必失败，且会把 MSVC 风格 CXXFLAGS/PCH 写进 makefile。
  2. 自带 mingw g++ 16.2 的静态 libstdc++ 损坏：`-O1/-Os` 下链接报 `std::__cxx11::basic_string` move ctor undefined（`-O0` 正常）→ 必须用 `-O0` 一步编译链接。
  3. `crt2.o/default-manifest.o` 在 `x86_64-w64-mingw32/sysroot/usr/lib`（gcc 搜索路径里没有），需 `-B` 指向该目录；AR 用 `Library/x86_64-w64-mingw32/bin/ar.exe`。
- 附带坑：本环境 bash 的 `rm` 被安全删除 shim 包装，verilated.mk 里的 `rm -f` 会因 shim 报错中断 make；绕法：不走 make，直接 g++ 一把编。
- GTKWave 启动必须把 `C:/Users/man/gtkwave/bin` 加进 PATH，否则 SVG loader DLL 依赖缺失直接 bail out。
- 产物：`obj_dir_comb/plm_sim.exe`、`waves.vcd`（707KB）；重建入口 `run_sim.sh`。

## 目录结构重构（tools→script）
- tools/ 改名 script/，config/ 移入 script/config/
- plm_gen.py 改造：--config 参数=配置集目录，CSV 查找顺序 <集>/config → <集>/ → 内置 script/config；RTL 固定输出 <集>/rtl（默认 config/rtl，--config config-1 则 config-1/rtl）
- flist.f 内用 ../../rtl/common 引手写公共模块，从任意目录调用均有效
- 验证状态：旧 exe（重构前构建）仿真 PASS；清空重建后链接被用户中止，未重测

## 2026-08-27 (续) - 极简多路复用拓扑 (2 控制器、x2 Mux 粒度) 配置、Comb 模式 RTL 与 VCS+Verdi FSDB 仿真生成
- **极简拓扑优化**：遵循“复用 Mux 最小粒度为 x2，最大同时工作控制器不超过 2 个，总控制器不超过 2 个”的精简规范。修改配置为：
  - **总控制器数 = 2**：`PCIe_x4` (ID=0) 和 `USB_x2` (ID=1)。
  - **模式简化 = 2**：Mode 0 (PCIe 独占，x4 模式) ➔ Mode 1 (PCIe_x2 + USB_x2，各占 2 条物理通道)。
  - **分组表现**：`lane0~1` 识别为直连组 (Group 0，无 BBM 消耗)；`lane2~3` 为 2 选 1 复用组 (Group 1，1 位 BBM 消耗)。由于各模式下 PCLK 来源完全固定，**不需要生成任何时钟无毛刺切换 Mux**，时钟网络达到绝对纯净。
- **Comb 模式生成**：运行 `plm_gen.py --config config_multi_modes --sel-mode comb`，成功在 `config_multi_modes/rtl/` 和 `tb/` 下生成了全套极简 RTL 设计与测试平台。
- **VCS & Verdi & FSDB 支持优化**：
  1. **测试平台升级**：在 `tb_pipe_lane_mapper.sv` 中，通过 `DUMP_FSDB` 编译宏条件编译，完美兼容常规 VCD 与 VCS 高效 FSDB 波形，并显式指定了 `"+all"` 选项（`$fsdbDumpvars(0, tb_pipe_lane_mapper, "+all")`），确保测试平台及 DUT 中的 SystemVerilog 结构体（`struct`）和多维数组信号能被全量、无死角地 Dump 到 FSDB 中。
  2. **工业级 Makefile 编写**：设计并部署 `config_multi_modes/tb/Makefile`：
     - **完美利用 flist.f**：编译时自动先 `cd ../rtl/` 再传入 `-f flist.f`，在保留并尊重原相对路径的同时，将编译输出 `-o ../tb/simv -l ../tb/compile.log` 回显到 `tb/`。
     - **去冗余**：清理了不必要的 `-lca` 选项。
     - **完美联调**：提供 `make verdi` 目标，启动时自动 `cd ../rtl`，传入 `-f flist.f` 和 TB 文件，确保在加载 FSDB 波形（`../tb/waves.fsdb`）的同时，能够百分之百无报错、完美完整地加载 RTL 层次源码和跳转。
- **软件定义芯片架构升级：可配置化的 `tie_off` 约束列引入**：
  应最高安全架构规范，我们在 `pipe_signals.csv` 配置文件中**首创性地新增了 `tie_off` 选项列**，实现了完全软件定义 unused lanes 的硬件极性归一化。
  - **配置映射规范**：通过在 CSV 的 `tie_off` 列配置 `0`（普通数据 tie 0）、`1`（电气安全信号 tie 1，如 `rxelecidle` 等）、或 `lane0`（握手同步对齐，如 `phystatus`），赋予了芯片设计者极高自由度的软件定义能力。
  - **RTL 编译自适应**：在 `plm_gen.py` 内部重构了 `gen_data_p2m` 函数。通过解析 `Signal.tie_off` 属性，**动态生成 `{c.lname}_safe_p2m_align` 结构体例化初值**。当 `tie_off` 标记为 `lane0` 时自动在例化时并联输入端口 `phy_phy2mac[{base_lane}].{sig_name}`，直接解决隐式敏感列表盲区，同时原生注入 `onehot_mux` 的 `.SAFE` 极性默认值，免除冗余 assignment。
  - **TB 断言自适应**：重构了 `gen_tb` 函数。自动在 TB 自校验程序中，根据每个信号的 `tie_off` 属性，**动态产生 `exp_tie` 的每位预期极性赋值与动态跟从判定断言**。这使得该测试机制完美泛化到任何拓扑、模式和新增信号中，无需任何人工硬编码维护。
